import styled from 'styled-components'

export const Container = styled.div`
    h1{
        color: var(--light);
        text-align: center;
        font-weight: 600;
        margin-top: 2rem;
    }
`